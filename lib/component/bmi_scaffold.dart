import 'package:flutter/material.dart';

class BMIScaffold extends Scaffold {
  BMIScaffold(Widget body)
      : super(
      appBar: AppBar(
        title: Text('BMI Calculator'),
      ),
      body: body);
}
